# 05-MERCH — Open Flags & Missing Information

Per stage policy (and root `CLAUDE.md`): **flag gaps, do not guess.** These items block
`READY TO LIST` and need Carter's input. Nothing here is invented — where a value is
unknown it is left `TBD`.

## 🚩 BLOCKER — the "attached list" of products was not attached
The task references *"5 Divine Archives items + 5 Pip's Orchard items (see attached
list)."* **No product list was actually attached to the request I received.** Rather than
stall the whole stage, I drafted **10 proposed products** so the scaffolding, briefs,
copy, and `/shop` page exist and are reviewable — but:

- Every proposed product is marked **`DRAFT · PROPOSED (list not received)`**.
- The Divine Archives five are each grounded in a **real published chapter** (verified in
  the chapter text), so they satisfy Non-Negotiable #2 regardless.
- The Pip five follow the **locked character** and standard orchard/seasonal merch types.
- **Action for Carter:** send the real list. I'll swap/replace any proposed product to
  match it (keeping the grounding + character rules). Do not treat these ten as final
  selections.

## 🚩 POD supplier not chosen
Supplier (Printful / Printify / Gelato / other) is undecided. Affects base cost, product
catalog, print method, and lead time. Per-product briefs note *feasibility by product
type*, not a locked vendor. **Need:** supplier decision, especially for non-flat-print
items.

- **Plush** (Pip): not standard POD — needs custom-manufacture / MOQ, not a print vendor.
- **Enamel pins** (Pip): custom manufacture or a pin-specialist POD, not the apparel vendor.
- **Die-cut stickers / sticker sheets:** many POD vendors do these, but sheet layouts vary.

## 🚩 Pricing / SKUs — all TBD
No SRP, margin target, or SKU scheme is set. Pricing depends on the chosen supplier's base
cost. Every brief lists price and SKU as `TBD`. **Need:** margin target + SKU convention
(e.g. `DA-TEE-EGY-01`, `PIP-STK-01`).

## 🚩 Domain / storefront structure unresolved
Two sub-brands, one Shopify store. Unresolved:
- Does Pip's Orchard sell under **getconexto.com** (e.g. `/shop` + `/pips-orchard`), a
  **separate domain**, or purely via Shopify's own storefront?
- The `/shop` page drafted here is for the **Divine Archives** static site only. **No Pip
  public page is drafted** pending this decision.
- No DNS/domain change has been or will be made without explicit instruction (root
  `CLAUDE.md` policy).

## 🚩 Shopify Buy Button — placeholder only
The `/shop` draft embeds a **`TODO` placeholder** where the Shopify Buy Button snippet
goes. The real snippet needs a **live Shopify product ID**, which doesn't exist yet
(nothing is listed). **Need:** live product IDs after products are created in Shopify
(post-review).

## 🚩 Pip source art does not exist yet
The locked Pip reference is described in `CLAUDE.md`, but **no actual Pip reference
image/illustration file is in the repo.** Briefs and copy are written to the spec; the
mockups folder is empty by design. **Need:** the canonical Pip reference art (or approval
to commission it) before any Pip mockup can be produced — and each Pip asset then goes
through the character-consistency review flag first.

## 🚩 Living-tradition sensitivity review (Divine Archives)
Some proposed DA designs draw on **living traditions** (e.g. Norse runes, the Kabbalistic
Tree of Life, the Buddhist Dharmachakra). These are grounded in published chapters, but
selling sacred symbols as merch is a **respect/appropriation judgment call** that belongs
to Carter, not this stage. Flagged for the review pass, not blocked.
