# 05-MERCH — Open Flags & Missing Information

Per stage policy: **flag gaps, do not guess.** Updated 2026-08-18 after Carter's confirmations
(final product list, colors, sacred symbols, Printify swaps). ✅ = resolved · 🚩 = still open.

## ✅ RESOLVED this round
- **Final product list received** — the 10 PROPOSED placeholders are replaced with Carter's
  final list. Briefs, copy, filenames, and the `/shop` cards now reflect it.
- **Pip color = brown/golden onion, FINAL.** "Moss-green" retired. No longer flagged.
- **Sacred symbols approved** (Tree of Life, Dharmachakra) with **attributed educational
  framing**. DA-04/DA-05 off HELD. DA-03 (runes) + DA-05 (Tree of Life/Dharmachakra) each
  carry a **mandatory attribution line** in the listing — enforced in the briefs + copy.
- **Printify swaps confirmed final** — pin-back button (not enamel), throw-pillow logic moot
  now that PIP-03 is a water bottle. All 10 products verified against Printify's catalog.

## 🚩 OPEN — Pip's "friend group" is not defined
`PIP-01` (sticker pack) and `PIP-03` (water bottle) call for "Pip + friend group." **Only Pip
has a locked reference** — the friends have no design, species, names, or colors on file.
**Do not invent them.** Mockups for these show **Pip only** with clearly-marked placeholder
slots for the friends. **Need from Carter:** reference art or a description per friend.

## 🚩 OPEN — "Pip and the…" tote must match real books
`PIP-04` is a storybook-**cover series**. To stay grounded to the real brand (and the Amazon
KDP books), the cover titles/scenes should mirror the **actual published Pip books** — not
invented titles. **Need from Carter:** the KDP book titles/covers to base the series on. Mockup
uses a generic "Pip and the …" placeholder title until then.

## 🚩 OPEN — Pip reference image files not yet in the repo
The locked spec lives in `pip-reference/REFERENCE.md`, but the **actual Pip art files** aren't
committed (Carter is sending separately; not blocking). Every Pip mockup currently uses a
**labeled placeholder silhouette**, not real Pip art — real art drops in, clears the character
flag, then replaces the placeholder.

## 🚩 OPEN — pricing / SKUs (TBD, Carter sending separately)
No SRP, margin, or SKU scheme yet — Carter is sending separately; not blocking. Every brief
lists price/SKU as `TBD`.

## 🚩 OPEN — storefront / domain structure
Pip's Orchard has TikTok / Facebook / YouTube + Amazon KDP books; a Pip storefront would link
from those. Unresolved: getconexto.com section vs. separate Pip domain vs. Shopify storefront.
The `/shop` draft is Divine Archives only; no Pip public page drafted. No DNS change without
instruction.

## 🚩 OPEN — Shopify Buy Button placeholder
`/shop` draft embeds a `TODO`; the real snippet needs a **live Shopify product ID** (created
post-review).

## Note — mockups are layout/design drafts, not final art
Mockups in `../mockups/` are design/placement drafts for review. DA mockups show real design
artwork (site data, emblem, runic ring, sefirot diagram). Pip mockups show product layouts with
**placeholder character art** pending the real files + character review. Nothing is print-ready.
